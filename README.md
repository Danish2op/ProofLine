# Proofline

Proofline is a TypeScript monorepo.

## Requirements

- Node.js 22 LTS
- pnpm 11.16.0

Install dependencies with `pnpm install --frozen-lockfile`, then run `pnpm test`.

Copy `.env.example` to `.env.local` and provide values only for local development.
